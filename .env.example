# Postgres connection string (Neon, Supabase, or local Postgres)
DATABASE_URL=postgres://user:password@host:5432/dbname

# Set to "false" only for local Postgres without SSL. Hosted providers
# (Neon, Supabase) require SSL, so leave unset or "true" in production.
DATABASE_SSL=true

PORT=3000
